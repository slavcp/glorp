{
	"type": "module",
	"scripts": {
		"esbuild": "node esbuild.config.mjs",
		"dev": "pnpm run esbuild && cargo build --workspace && node postbuild.js debug && cargo run --features editor-ignore",
		"build": "pnpm run esbuild && cargo build --workspace --release --features packaged,editor-ignore --target x86_64-pc-windows-msvc && node postbuild.js x86_64-pc-windows-msvc/release",
		"build:auto-update": "pnpm run esbuild && cargo build --workspace --release --features packaged,auto-update,editor-ignore --target x86_64-pc-windows-msvc && node postbuild.js x86_64-pc-windows-msvc/release",
		"package": "pnpm run build:auto-update && wix build ./resources/installer_script.wxs -ext WixToolset.Util.wixext -o target/glorp-setup-x86_64.msi"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.15",
		"esbuild": "^0.28.0"
	},
	"dependencies": {
		"marked": "^18.0.4"
	},
	"packageManager": "pnpm@11.1.3"
}