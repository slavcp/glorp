[package]
name = "webview-dll"
version = "0.1.0"
edition = "2024"

[lib]
name = "webview"
crate-type = ["cdylib"]

[dependencies]
windows = { version = "0.59.*", features = ["Win32_UI_Accessibility"] }
